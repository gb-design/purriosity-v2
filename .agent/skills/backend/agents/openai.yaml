interface:
  display_name: "Backend"
  short_description: "Deliver safe Supabase/Postgres schema and policy changes"
  default_prompt: "Use $backend to implement this schema or API contract change with migration safety and RLS checks."
policy:
  allow_implicit_invocation: true
