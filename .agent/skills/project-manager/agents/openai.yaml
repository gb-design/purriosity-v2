interface:
  display_name: "Project Manager"
  short_description: "Orchestrate multi-track delivery and release decisions"
  default_prompt: "Use $project-manager to plan and coordinate this feature across frontend, backend, QA, content, and DevOps."
policy:
  allow_implicit_invocation: true
