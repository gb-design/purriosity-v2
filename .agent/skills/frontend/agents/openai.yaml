interface:
  display_name: "Frontend"
  short_description: "Build and refine React/Vite user interfaces safely"
  default_prompt: "Use $frontend to implement this UI change with accessibility and responsive validation."
policy:
  allow_implicit_invocation: true
